<Window x:Class="HGEngineHelper.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HGEngineHelper"
        mc:Ignorable="d"
        Title="HGEngine Helper" Height="180" Width="250" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="50"/>
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" FontWeight="Bold" FontSize="20" Height="30" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            HGEngine Helper
        </TextBlock>
        <Button Name="btnNewProject" Click="btnNewProject_Click" Grid.Row="1" Height="40" Width="200">
            New Project
        </Button>
        <Button Name="btnOpenProject" Click="btnOpenProject_Click" Grid.Row="2" Height="40" Width="200">
            Open Project
        </Button>
    </Grid>
</Window>
